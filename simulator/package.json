{
  "name": "mush2-simulator",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Protocol Simulator (Virtual Device) — FASE 1. Emula el firmware: publica telemetría/status conforme al contrato canónico y responde a comandos con ACK (ADR-030).",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js --config jest.config.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mqtt": "^5.15.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
