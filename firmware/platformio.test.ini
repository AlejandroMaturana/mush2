; Config de test host-only para firmware. NO usada por `pio run` (build de dispositivo).
; Uso: pio test -c platformio.test.ini -e native
[platformio]
src_dir = test

[env:native]
platform = native
test_framework = unity
test_filter = test_channel_mapping
build_flags =
    -Isrc
