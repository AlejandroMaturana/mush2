# ── Empty password file template ─────────────────────────────────
# Copy to password_file and generate credentials:
#   mosquitto_passwd -b docker/mosquitto/dev/password_file <user> <pass>
#
# Existing users (for reference when recreating):
#   dev_device_dev_001  — Test user for vertical slice validation
#   backend_bridge      — Backend bridge service
#
# NEVER commit real credentials to version control.
